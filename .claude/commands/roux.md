Hand this to ROUX, head of marketing.

Invoke the **roux** agent with everything below, plus whatever context from this
conversation it needs to answer without re-asking. If nothing follows the command, ask
Evan what he wants ROUX's read on — one line, then stop.

$ARGUMENTS

---

**When ROUX comes back:**

ROUX runs as a subagent and **cannot call another agent** — it will end with a
`**Next:** <agent> — <what and why>` line instead. That line is an instruction to you,
not to Evan. Dispatch it, unless doing so would spend money, publish something, or
change a live system — those wait for Evan.

Report ROUX's answer to Evan directly. Lead with the call, then the reasoning. Do not
summarize ROUX into blandness; the whole point of asking is the opinion.

If ROUX's answer rests on a number, check it names a source. If it doesn't, say so rather
than passing it on.
