# Retired skills

Skills taken out of `my-skills/` because they overlapped a newer HPC skill and could fire on the
same request. Kept here, never deleted. Each folder holds the old `instructions.md` and its
`.claude/commands/` file as `command.md`. To bring one back, move both back.

| Skill | Retired | Why | Use instead |
|---|---|---|---|
| `weekly-check` | 2026-09-25 (Evan) | Fired on "how are we tracking" like `month-plan`; read Stripe and appointments, not Shopify | `month-plan` scoreboard, `/improve` |
| `write-an-ad` | 2026-09-25 (Evan) | Fired on "Meta ad"; no HPC copy rules, no rubric-check | `hpc-ad-creative`, `meta-ad-build` (Maya) |
